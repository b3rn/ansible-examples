---
- name: Terminate instances
  hosts: demo_{{ tower_user_name }}_windows
  tasks:
    - name: Terminate instances that were previously launched
      ec2:
        state: absent
        instance_ids: "{{ ec2_id }}"
      delegate_to: localhost
